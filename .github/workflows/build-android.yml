name: Build Android APK

on:
  workflow_dispatch: # Allows you to manually trigger the build from the GitHub Actions tab
  push:
    branches:
      - main
      - master

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '21' # Modern Android builds require Java 21

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install Node Modules
        run: bun install

      - name: Generate App Icons
        run: |
          mkdir -p assets
          cp public/logo.png assets/icon.png
          cp public/hero-illustration.png assets/splash.png
          bunx @capacitor/assets generate

      - name: Sync Capacitor
        run: npx @capacitor/cli sync android

      - name: Build Android Debug APK
        working-directory: ./android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: waypoint-android-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
